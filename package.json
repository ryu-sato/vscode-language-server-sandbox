{
	"name": "sample-language-server",
	"displayName": "Sample Language Server",
	"description": "A sample Language Server implementation",
	"version": "0.0.1",
	"private": true,
	"publisher": "sample",
	"main": "./client/out/extention.js",
	"activationEvents": [
		"onStartupFinished"
	],
	"contributes": {
		"commands": []
	},
	"devDependencies": {
		"typescript": "^5.9.3"
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc --build",
		"watch": "tsc --build --watch",
		"lint": "eslint",
		"postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
		"test": "sh ./scripts/e2e.sh"
	}
}
